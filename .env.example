LLM_PROVIDER=openai

# Required when LLM_PROVIDER=openai
OPENAI_API_KEY=

# OpenAI model for structured extraction
OPENAI_MODEL=gpt-5.4-mini

# SQLite database URL (default: sqlite:///deliverylint.db)
DATABASE_URL=sqlite:///deliverylint.db
